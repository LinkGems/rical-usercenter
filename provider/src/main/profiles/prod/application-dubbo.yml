dubbo:
  application:
    # 应用名称
    name: rical-backend-provider
    id: rical-backend-provider
  registry:
    # 注册中心地址
    address: nacos://172.21.0.14:8848/
  protocol:
    # 传输协议
    name: dubbo
    # ip不可为127.0.0.1 -> 本机ip
    host: 172.21.0.14
    # 随机端口
    port: 20881
